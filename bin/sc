#!/usr/bin/env node


var program = require('commander'),
    pkg = require('../package.json');


program
    .version(pkg.version)
    .usage('[options] [dir]')
    .option('-b, --build', 'build the dir')
    .parse(process.argv);


// vim ft=javascript
